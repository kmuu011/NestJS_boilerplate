name: Deploy

on:
  push:
    branches: [ master ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Sentry Release
        uses: getsentry/action-release@v1.0.0
        env:
          SENTRY_AUTH_TOKEN: 3ea8e349fc524d50bde6cfa317df993bfe661f07438f480cb96e2be54f87ae4f
          SENTRY_ORG: kang-ma
          SENTRY_PROJECT: nestjs_boilerplate
        with:
          environment: production